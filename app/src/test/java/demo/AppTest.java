package demo;


import static org.junit.Assert.assertThrows;
import static org.junit.Assert.assertThat;

/**
 * AppTest
 */
public class AppTest {

    void test(){
        assertThat(null, null, null);
        asserThrows(null,null);
    }
}